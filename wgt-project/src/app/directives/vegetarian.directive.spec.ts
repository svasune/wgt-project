import { VegetarianDirective } from './vegetarian.directive';

describe('VegetarianDirective', () => {
  it('should create an instance', () => {
    const directive = new VegetarianDirective();
    expect(directive).toBeTruthy();
  });
});
